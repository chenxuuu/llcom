# LLCOM 重构冒烟测试清单

> 每个重构步骤完成后，按本清单逐项验证。全绿 → 继续下一步；有红 → 报告并回退/修复该步 commit。
> 测试环境：本机 VS2026（Visual Studio 18 Community），Windows。

## 构建命令（x64 Debug）

```bat
"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\amd64\MSBuild.exe" llcom\llcom.csproj -p:Configuration=Debug -p:Platform=x64
```

- [ ] 构建零 error（允许原有 warning）
- [ ] 生成 `llcom\bin\x64\Debug\llcom.exe`

## 一、主窗口基础

- [ ] 程序启动，主窗口正常显示（标题"LLCOM - 能跑Lua脚本的串口调试工具"）
- [ ] 无异常弹窗、无崩溃
- [ ] 关闭窗口程序完全退出（无残留进程）
- [ ] 再次启动，窗口位置/大小与上次一致

## 二、串口功能

- [ ] 串口下拉列表能列出本机 COM 口（无串口时打开按钮置灰）
- [ ] 点击"刷新串口"能刷新列表
- [ ] 打开串口成功，状态栏显示"状态：已打开"、按钮变为"关闭串口"
- [ ] 串口下拉框在打开后禁用
- [ ] 关闭串口成功，按钮恢复"打开串口"
- [ ] 拔插 USB 转串口设备 → 列表自动刷新（热插拔）
- [ ] 波特率：切换预设值生效；选择"自定义"弹出输入框，非法输入有提示
- [ ] 波特率等串口参数重启后保留
- [ ] RTS / DTR 复选框可切换（数据区下方）

## 三、数据收发与显示

- [ ] 发送框输入文本，Ctrl+Enter 或点"发送"按钮可发送
- [ ] 勾选 HEX 发送后，输入 hex 字符串按字节发送
- [ ] "添加回车换行"选项生效（发送内容尾部附加 \r\n）
- [ ] 未打开串口时点发送 → 自动尝试打开串口，成功后补发
- [ ] 接收数据显示在左侧（时间戳、方向箭头、数据）
- [ ] HEX 显示三态复选框：字符串 / HEX / 两者同时显示
- [ ] 分包模式（timeout≥0）与流式模式切换后显示正常
- [ ] 发送/接收字节计数正确，右键计数可清零
- [ ] "清空日志"按钮生效
- [ ] 锁定滚动按钮（悬停出现）生效
- [ ] 保存日志按钮：文本模式与分包模式均能导出 .log 文件
- [ ] 终端模式：点击数据区直接输入可发送；Ctrl+A~Z 发送控制字符
- [ ] 回显开关：取消"显示发送"后发送数据不再显示
- [ ] 符号显示（\r\n 等控制符可视化）开关生效
- [ ] "禁用日志"开关：勾选后接收数据不再显示

## 四、快捷发送区

- [ ] 添加/删除条目生效
- [ ] 双击发送按钮可改名
- [ ] 右键序号可改位置、留空删除条目
- [ ] 右键列表名称可重命名当前页
- [ ] 10 页切换正常，各页数据独立、重启后保留
- [ ] HEX 复选框生效（按 hex 发送）
- [ ] 一键清空（需输入 YES 确认）
- [ ] 导出 JSON / 导入 JSON 正常
- [ ] 从 SSCOM 配置文件导入正常
- [ ] 单条"脚本"图标：左键选择接收转换脚本、右键清除
- [ ] 单条"参数"图标：设置/清除接收脚本参数

## 五、Lua 脚本

- [ ] 脚本列表显示 `user_script_run` 下的 .lua 文件
- [ ] 切换文件自动保存上一个文件
- [ ] 外部编辑器修改文件后，切回窗口自动重新加载
- [ ] 新建脚本：无名字/重名有提示，创建成功并打开
- [ ] 语法高亮、代码折叠（AvalonEdit）正常
- [ ] 运行 example.lua：日志区显示运行输出
- [ ] 运行/停止/暂停按钮状态切换正确
- [ ] 单行命令输入框可执行 Lua 代码（如 `print("hello")`）
- [ ] 定时器、协程任务（sys.wait / sys.taskInit）工作正常
- [ ] Lua 日志写入 `user_script_run/logs/log.txt`
- [ ] 发送转换脚本：设置窗口修改后，主窗口发送时按脚本处理（如"加上换行回车"）
- [ ] 接收转换脚本：收到数据时按脚本处理（如"绘制曲线"）
- [ ] channel-demo.lua 的通用通道收发正常

## 六、工具页（右侧"工具"Tab）

- [ ] TCP 客户端：TCP 连接/断开/发送/接收；UDP 模式；SSL 模式；自动重连开关
- [ ] 本地 TCP 服务器：监听/停止/广播/多客户端/踢下线信息
- [ ] 本地 UDP 服务器：监听/停止/收发
- [ ] 公网 TCP 测试（netlab）：创建 TCP/UDP/SSL/IPv6 服务器、客户端连接、数据收发
- [ ] MQTT：连接/订阅/发布（含 QoS）、TLS、WebSocket、证书配置
- [ ] 编码转换：String↔Hex / Base64 / URL / HTML / Unicode / MD5 / SHA1/256/512
- [ ] 乱码修复：输入乱码文本，列表给出修复结果，点击复制
- [ ] 绘制曲线：Lua 脚本 `apiAddPoint` 画点、清空、主题切换
- [ ] WinUSB：列表刷新、连接/断开、收发（无设备时至少不崩溃）
- [ ] 串口监听：进程+COM 选择、开始/停止监听（x64 下 DLL 不兼容则按钮禁用并有提示）
- [ ] 在线脚本：列表加载、详情页、下载保存到 `user_script_run`

## 七、设置窗口（"更多设置"）

- [ ] 数据位（5~8）、停止位（1/1.5/2）、校验位（无/奇/偶/Mark/Space）切换生效
- [ ] 编码下拉（按代码页排序）切换生效，重启保留
- [ ] 发送转换脚本编辑：新建/保存/测试运行（显示 HEX 与文本结果）
- [ ] 接收转换脚本编辑：新建/保存/测试运行
- [ ] 窗口关闭后隐藏而非退出（主窗口关闭时才退出）

## 八、语言与其他

- [ ] 右上角语言菜单切换 简体中文 / English，界面即时变化，重启保留
- [ ] 窗口置顶开关生效（设置窗口内）
- [ ] 关于页显示版本号；"检查更新"不崩溃（网络不通时显示失败提示）
- [ ] `settings.json` 与 `settings.json.bakup` 在软件目录生成
- [ ] 串口日志写入 `logs/log.txt`（按天滚动）
- [ ] 首次运行生成完整目录结构（core_script、user_script_run 等）

## 九、回归重点（重构步骤重点关注）

| 重构步骤 | 重点回归项 |
|---|---|
| Step 2 Global 拆分 | 语言切换、配置加载、HEX 转换、多开检测、目录初始化 |
| Step 3 Settings | 所有设置持久化、旧 settings.json 兼容、10 页快捷列表 |
| Step 4 SerialPortService | 串口全部交互、热插拔、Lua uart 通道 |
| Step 5 MainViewModel | 串口区全部交互、缓存发送、终端模式 |
| Step 6 QuickSend | 快捷发送全部子项 |
| Step 7 LuaEditor | 主窗口 Lua + 设置窗口脚本编辑 |
| Step 8 DataShow | 显示模式、性能（115200 持续收发）、保存日志 |
| Step 9 语法整理 | 全量冒烟 |
