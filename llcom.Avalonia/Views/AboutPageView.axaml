<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:llcom.Avalonia.ViewModels"
             x:Class="llcom.Avalonia.Views.AboutPageView"
             x:DataType="vm:AboutViewModel">
    <ScrollViewer>
        <StackPanel Spacing="8" Margin="16" MaxWidth="400">
            <TextBlock Text="LLCOM" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{DynamicResource AboutSubTitle}" HorizontalAlignment="Center"/>
            <Separator/>
            <TextBlock Text="{Binding Version, StringFormat='Version: {0}'}" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Platform, StringFormat='Platform: {0}'}" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Framework, StringFormat='Runtime: {0}'}" TextWrapping="Wrap"/>
            <Separator/>
            <TextBlock Text="Developers: chenxuuu, whc2001" TextWrapping="Wrap"/>
            <Button Content="{DynamicResource AboutGitHubButton}" Command="{Binding OpenGitHubCommand}"/>
            <Button Content="{DynamicResource AboutFeedBackButton}" Command="{Binding OpenIssueCommand}"/>
            <Button Content="{DynamicResource LuaApiReference}" Command="{Binding OpenApiDocCommand}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
